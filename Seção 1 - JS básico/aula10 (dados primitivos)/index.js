//Dados primitivos: String, number, undefined,
// null, boolean e symbol(ver posteriormente)


//String
let string = 'string';
let String = "String";
let stringCraze = `string`;


//Number (nao tem diferença de int para double/float);
let numero1 = 10;
let numero2 = 10.1;

//Undefined (não aponta para lugar nenhuma na memória)
let nomeAluno;
let teste = undefined; //Prática errada

//UNDEFINED NÃO É IGUAL A NULL
//Undefined não tem valor declarado, já o null, explicitamente está
//declarado que o valor é nulo;

//Null (não aponta para lugar nenhuma na memória)
let sobrenomeAluno = null;

//Boolean
let booleanTrue = true;
let booleanFalse = false;